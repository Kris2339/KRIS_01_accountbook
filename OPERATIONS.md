# 운영 및 복구 절차

2026-09-22 사용자 요청으로 UI는 개편 전 `2a8c7f7`로 복원했습니다. 저장 엔진은 유지하며 `legacy-storage.js`가 원래 저장/로드 함수와 연결됩니다. 파랑·초록 UI JS/CSS는 활성 자산에서 제거했습니다. `accountbook-classic-pending`은 원래 UI에서 저장할 때의 동기식 복구 사본이며 IndexedDB 확정 후 제거됩니다. 이전 localStorage와 2.x 초안은 삭제하지 않습니다.

## 최초 데이터 복구

새 버전을 배포하기 전 두 휴대폰에서 현재 앱을 열고 설정의 `내보내기`를 실행합니다. 앱 삭제, 사이트 데이터 삭제, 브라우저 캐시 초기화는 내보내기가 끝날 때까지 하지 않습니다.

1. 두 JSON 파일의 거래 수와 마지막 거래일을 확인합니다.
2. 더 완전한 파일을 기준으로 선택합니다.
3. 다른 파일에만 있는 거래가 있으면 거래 `id` 기준으로 병합합니다.
4. 새 앱의 설정 → 파일에서 합치기로 병합하고 같은 항목의 차이는 충돌 확인에서 선택합니다.
5. 내역과 합계를 확인한 뒤 다시 동기화를 실행합니다.

기존 localStorage는 지우지 않습니다. 설정의 이전 기기 사본에서 이전 앱 및 새 앱의 가져오기·충돌 전 사본을 내려받을 수 있습니다. 변경은 IndexedDB에 먼저 저장하고 서버와 병합합니다.

2026-09-22에는 서버 850건에 기기 사본에만 있던 22건을 추가해 872건을 검증했습니다. 복구 전후 백업과 증거는 저장소 밖 accountbook-backups에 보관했습니다. 이후 사용자 입력에 따라 거래 수는 변합니다.

## 정기 백업

- 매월 말: 앱의 전체 내보내기를 실행해 별도 저장 매체에 보관합니다.
- 배포 전: 전체 JSON 내보내기와 D1 전체 SQL 내보내기를 각각 한 번 만듭니다.
- 사고 발생 시: 최근 JSON을 먼저 확인하고, 필요한 경우 D1 Time Travel로 장애 직전 시점을 복원합니다.

```powershell
wrangler d1 export hkaccount-production --remote --output=hkaccount-backup.sql
```

## 보안 점검

- `APP_PIN`과 `SESSION_SECRET`은 Worker 비밀 변수로 설정하고 저장소에 커밋하지 않습니다.
- 로그인하지 않은 상태에서 `/api/data`와 `/api/health`가 401을 반환하는지 확인합니다.
- 잘못된 번호를 5회 입력하면 10분 동안 429로 차단되는지 확인합니다.
- 로그인 쿠키는 `HttpOnly`, `Secure`, `SameSite=Strict` 속성을 유지합니다.
- 저장소의 문서, SQL, `.git` 파일이 웹에서 404인지 확인합니다.

## 장애 대응

1. 입력을 잠시 중단합니다.
2. 정상인 휴대폰에서 즉시 전체 JSON을 내보냅니다.
3. Cloudflare D1의 현재 상태와 Time Travel 북마크를 확인합니다.
4. 복원 전 현재 D1도 SQL로 내보냅니다.
5. 복원 후 6월 말 거래, 최근 거래, 총 거래 수를 비교합니다.

## 2.0 배포 및 롤백

운영 SQL을 저장소 밖으로 내보낸 뒤 `npx wrangler d1 migrations apply hkaccount-production --remote`, `npx wrangler deploy`를 실행합니다. 배포 전후 거래 수와 내용을 비교합니다. 구버전에는 API 426이 반환되므로 휴대폰 앱을 완전히 닫고 다시 엽니다. 사이트 데이터는 삭제하지 않습니다.

이전 Worker에는 850건 제한과 구형 동기화 문제가 있으므로 그대로 롤백하지 않습니다. 장애 시 2.0 데이터 보호 프로토콜을 유지하며 수정하거나 쓰기를 잠시 제한합니다. 0003은 기존 거래를 바꾸지 않고 이후 변경 이력을 추가합니다. 과거 변경 이력까지 생성하지는 않습니다.

## 검증과 제한

로컬 D1에서 Chrome/WebKit으로 연속 입력, 메모·초안, 화면 겹침, 오프라인·재접속, 동시 저장, 응답 유실, 충돌, 저장소 실패, 1,000건 저장, 잘못된 입력 차단을 검증합니다. 실제 iPhone 키보드와 두 실기기의 네트워크는 별도 확인 대상입니다.

현재 공유 번호 인증은 소규모 공동 사용용입니다. 여러 가구에 공개하기 전 별도 계정·가구별 권한 분리가 필요합니다. 기기 저장 공간을 삭제하면 미전송 기록은 기기 백업 없이는 복구할 수 없습니다.
